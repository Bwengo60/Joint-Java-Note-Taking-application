import java.util.Scanner;

public class RegistrationApp {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        while (true) {
            System.out.println("1. Register with Email");
            System.out.println("2. Register with iCloud");
            System.out.println("3. Register with Facebook");
            System.out.println("4. Exit");
            System.out.print("Choose an option: ");
            int choice = scanner.nextInt();

            switch (choice) {
                case 1:
                    registerWithEmail(scanner);
                    break;
                case 2:
                    registerWithICloud(scanner);
                    break;
                case 3:
                    registerWithFacebook(scanner);
                    break;
                case 4:
                    System.out.println("Exiting registration.");
                    System.exit(0);
                default:
                    System.out.println("Invalid option. Please try again.");
            }
        }
    }

    private static void registerWithEmail(Scanner scanner) {
        System.out.print("Enter your email: ");
        String email = scanner.next();

        // You should implement a method to store the user information in your data storage system.
        boolean registrationSuccess = storeUserInformation(email, "Email");

        if (registrationSuccess) {
            System.out.println("Registration with email successful!");
        } else {
            System.out.println("Registration with email failed. Please try a different email or check the provided information.");
        }
    }

    private static void registerWithICloud(Scanner scanner) {
        System.out.print("Enter your iCloud username: ");
        String iCloud = scanner.next();

        // You should implement a method to store the user information in your data storage system.
        boolean registrationSuccess = storeUserInformation(iCloud, "iCloud");

        if (registrationSuccess) {
            System.out.println("Registration with iCloud successful!");
        } else {
            System.out.println("Registration with iCloud failed. Please try a different iCloud username or check the provided information.");
        }
    }

    private static void registerWithFacebook(Scanner scanner) {
        System.out.print("Enter your Facebook account: ");
        String facebook = scanner.next();

        // You should implement a method to store the user information in your data storage system.
        boolean registrationSuccess = storeUserInformation(facebook, "Facebook");

        if (registrationSuccess) {
            System.out.println("Registration with Facebook successful!");
        } else {
            System.out.println("Registration with Facebook failed. Please try a different Facebook account or check the provided information.");
        }
    }

    private static boolean storeUserInformation(String account, String accountType) {
        // In a real application, you would save user information to your database.
        // For this example, we simply return true to simulate success.
        System.out.println("Storing user information for " + accountType + ": " + account);
        return true;
    }
}
